{
  "name": "x-viral-monitor",
  "version": "1.7.4",
  "private": true,
  "type": "module",
  "scripts": {
    "debug:grok": "node scripts/debug-grok.mjs",
    "build:dist": "node scripts/build-dist.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "playwright": "^1.59.1",
    "vitest": "^1.6.0"
  }
}
